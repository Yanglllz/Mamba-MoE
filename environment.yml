name: mamba_moe
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - numpy
  - scipy
  - scikit-image
  - tqdm
  - pyyaml
  - pytorch>=2.0
  - torchvision
  - pip:
      - einops

